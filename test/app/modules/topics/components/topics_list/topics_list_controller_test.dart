import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:time_tracker/app/modules/topics/topics_module.dart';

void main() {
  initModule(TopicsModule());
  // TopicsListController topicslist;
  //
  setUp(() {
    //     topicslist = TopicsModule.to.get<TopicsListController>();
  });

  group('TopicsListController Test', () {
    //   test("First Test", () {
    //     expect(topicslist, isInstanceOf<TopicsListController>());
    //   });

    //   test("Set Value", () {
    //     expect(topicslist.value, equals(0));
    //     topicslist.increment();
    //     expect(topicslist.value, equals(1));
    //   });
  });
}
