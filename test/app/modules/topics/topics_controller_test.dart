import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:time_tracker/app/app_module.dart';

void main() {
  initModule(AppModule());
  // TopicsController topics;
  //
  setUp(() {
    //     topics = AppModule.to.get<TopicsController>();
  });

  group('TopicsController Test', () {
    //   test("First Test", () {
    //     expect(topics, isInstanceOf<TopicsController>());
    //   });

    //   test("Set Value", () {
    //     expect(topics.value, equals(0));
    //     topics.increment();
    //     expect(topics.value, equals(1));
    //   });
  });
}
